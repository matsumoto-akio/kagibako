# カギバコ DESIGN.md

これから決めるデザイン案ではない。すでに動いているものから値を測って書き出した記録。
LPを作るときは、ここに書いてある数字をそのまま使う。

新しい色・新しいフォント・新しい装飾を足さない。足したくなったら、
それは既存のどこかに実在するかを先に確認する。

## 出典

| 出典 | 何を測ったか |
|---|---|
| `docs/screenshot.png` `-2.png` `-3.png` | アプリ実機のダークモード表示。色は全部ここから抽出 |
| `Sources/KagibakoApp/AppColor.swift` `AppFont.swift` ほか | 余白・文字サイズ・アイコン名の数値 |
| note サムネイル2枚（1733×907） | 見出しの組み方・アクセントの当て方 |
| `scripts/make_icon.swift` | アプリアイコンの配色 |
| `x-research-ai/style_profile.md` | 文章の書き方 |

---

## 1. 色

### 1.1 アプリ画面の実測値

macOS ダークモードのスクリーンショットから抽出した値。

| 用途 | hex | 出どころ |
|---|---|---|
| ウインドウ背景 | `#242A2E` | 全画面の地の色 |
| 囲みブロックの背景 | `#2F3438` | 「やることは3つです」等の枠 |
| 起動画面の約束枠 | `#2D3236` | `.quaternary` 40% |
| チップ・コマンド枠の背景 | `#393F42` | `.quaternary` 100% |
| 注意ブロックの背景 | `#3E362E` | オレンジ 12% を重ねた結果 |
| 本文の文字 | `#DEDFDF` | |
| 補足の文字 | `#9C9FA1` | `.secondary` |
| アクセント | `#FF9230` | macOS の systemOrange（ダーク） |
| リンク | `#419CFF` | 「全部まとめて見る」 |
| 主ボタン（ウインドウ非アクティブ時） | `#343A3E` | アクティブ時はアクセント塗り |

### 1.2 note サムネイルの実測値

| 用途 | hex |
|---|---|
| 背景 | `#000000`（実測 `#010102`） |
| 文字 | `#FFFFFF` |
| アクセント1語（24ファイル記事） | `#F98517` |
| アクセント1語（指示書テンプレ記事） | `#F67E12` |
| タグの枠線 | `#FFFFFF` / 2px相当 / 塗りなし |

### 1.3 オレンジが3つあるが、色相は同じ

`#FF9230`（アプリ）・`#F98517`・`#F67E12`（サムネ2枚）は、色相がどれも28〜29度。
違うのは明るさと鮮やかさだけ。並べてもズレて見えない。

アプリ側の `#FF9230` は macOS が決めている色で、こちらでは指定していない。
スクリーンショットをLPに貼るとき、この色を直す必要はない。

### 1.4 LPで使う色

**新しい色を作らず、上の表からそのまま持ってくる。**

```css
:root {
  --bg:            #000000;  /* ページ全体。サムネと同じ黒 */
  --surface:       #242A2E;  /* 囲みブロック。アプリのウインドウ背景を流用 */
  --surface-inner: #2F3438;  /* 囲みの中のさらに内側 */
  --chip:          #393F42;  /* タグ・コマンド表示 */
  --note-bg:       #3E362E;  /* 注意ブロック */
  --text:          #FFFFFF;
  --text-sub:      #9C9FA1;
  --accent:        #F98517;  /* カギバコ記事のサムネの値を正とする */
  --border:        #FFFFFF;  /* 枠線。不透明度で調整する */
}
```

### 1.5 オレンジの当て方

**1画面に1箇所だけ。**（スクロールで見えている1画面分を1単位と数える）

- サムネ1枚に対してオレンジは1語（「24」「テンプレ」）
- アプリの結果画面でオレンジになるのは、いちばん大きい件数の数字だけ
- LPのヒーローも同じ。数字1つ、または単語1つ

例外は主ボタン。ボタンはオレンジ塗り＋白文字にする（アプリと同じ）。
それ以外にオレンジを増やすと、どこを見ればいいか分からなくなる。

### 1.6 状態を示す色

危険度の3層でだけ色を使う。装飾には使わない。

| 層 | 色 | アプリでの表示 |
|---|---|---|
| いますぐ対処 | オレンジ | `exclamationmark.triangle.fill` |
| 確認した方がいい | 黄 | `eye` |
| そのままでよい | 補足色 `#9C9FA1` | `checkmark.circle` |
| 対処不要（0件） | 緑 | `checkmark.circle.fill` |

赤は使わない。実際に起きているのは「置きっぱなし」であって、事故ではないため。

---

## 2. フォント

### 2.1 アプリが使っているもの

`Font.system` のみ。macOS が日本語をヒラギノ角ゴシック、英数字を SF Pro に振り分ける。
カスタムフォントは1つも入れていない。

3種類の使い分けだけがある。

| 種類 | 指定 | 使う場所 |
|---|---|---|
| 標準 | `Font.system(size:)` | 見出し・本文・補足の全部 |
| 等幅 | `design: .monospaced` | パス・コマンド・検出行 |
| 丸ゴシック | `design: .rounded` | 大きい数字（46pt / 34pt）だけ |

### 2.2 サムネイルが使っているもの

- 見出し：太いゴシック（ウエイトは Bold 以上）
- サブタイトル：同じゴシックを細めにして、字間を約 0.15em 空ける
- タグ：等幅。字間を約 0.1em 空ける

### 2.3 LPでの指定

```css
--font-sans: -apple-system, BlinkMacSystemFont, "Hiragino Sans",
             "Noto Sans JP", sans-serif;
--font-mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
```

Webフォントを読み込まない。表示が遅れる方が損が大きい。

丸ゴシック（`.rounded`）に相当するWebフォントは指定しない。
アプリで丸ゴシックにしていた大きい数字は、LPでは同じゴシックの Bold で置く。
数字だけ書体を変えると、フォントを1つ増やすことになる。

### 2.4 等幅を使う場所

アプリのルールをそのまま持ってくる。ここ以外で等幅を使わない。

- ファイルパス（`~/.zshrc` `~/.codex/shell_snapshots/`）
- ターミナルのコマンド
- 検出行の表示（`L473  OpenRouter  sk-or-v****(73文字)`）
- サムネ下部のタグ（`Claude Code / Codex`）

---

## 3. 文字サイズの階層

### 3.1 アプリの実測値

スクリーンショットの字面の高さから逆算して確認済み。単位は pt（1x で 1px）。

| 役割 | サイズ | 太さ | 使っている場所 |
|---|---|---|---|
| 主役の数字 | 46 | Bold（丸ゴ） | 「いますぐ対処」の件数 |
| 走査中の件数 | 34 | Semibold（丸ゴ） | スキャン中のファイル数 |
| アプリ名 | 30 | Bold | 起動画面 |
| 大見出し | 17 | Bold | 「調べられませんでした」「0件でした」 |
| 中見出し | 15 | Bold | 「件は、いますぐ対処した方がいいキーです」 |
| 本文 | 15 | Regular | 説明文 |
| ボタンのラベル | 13 | Semibold | 「このMacを調べる」 |
| 補足・手順・パス | 14 | Regular | **アプリ内の最小** |

**14より小さい文字を使わない。** これは `AppFont.swift` に明記された決定。
標準の `.caption`（10pt）は、この内容を初めて読む人には小さすぎる。

もう1つ特徴がある。**中見出しと本文は同じ15ptで、違いは太さだけ。**
サイズの段を増やさずに、太さで差をつけている。

### 3.2 サムネイルの実測値

キャンバス 1733×907（比率 1.91:1）。

| 役割 | 字面の高さ | 見出しに対する比 |
|---|---|---|
| 見出し | 123〜149px | 1.00 |
| サブタイトル | 43〜49px | 約 0.33 |
| タグ | 29〜32px | 約 0.22 |

見出しのサイズはキャンバス高の 14〜16%。文字数に応じて、枠内に収まる最大まで上げている。
2枚で寸法が違うのはそのため。比率（1 : 0.33 : 0.22）は2枚とも揃っている。

### 3.3 LPで使うサイズ

アプリのUI文字とWebの本文文字は基準が違うので、そのまま px にはしない。
**本文を17pxに置き、上下の段も同じ比率で持ち上げる。**
比率と「最小より下は使わない」の原則だけを引き継ぐ。

| 役割 | LP | 太さ | 備考 |
|---|---|---|---|
| ヒーロー見出し | `clamp(32px, 7.5vw, 96px)` | 700 | サムネの組み方に従う |
| ヒーローのサブ | ヒーローの 0.33 | 500 | `letter-spacing: 0.15em` |
| ヒーローのタグ | ヒーローの 0.22 | 400 | 等幅・`letter-spacing: 0.1em` |
| セクション見出し | 28px | 700 | |
| 主役の数字 | 56px | 700 | |
| 中見出し | 19px | 700 | 本文と2px差だけ |
| 本文 | 17px | 400 | |
| 補足・手順・パス | 15px | 400 | **LPの最小** |

15pxより小さい文字をLPに置かない。注釈も脚注もクレジットも、下限は15px。

行間は本文 1.8、見出し 1.35。日本語の長文を読ませるため。

---

## 4. 余白

### 4.1 アプリで実際に使われている数字

```
2 / 3 / 4 / 5 / 6 / 8 / 10 / 12 / 16 / 18 / 20 / 24 / 28
```

| 場所 | 値 |
|---|---|
| 画面の外周 | 28 |
| 大ブロックどうしの間隔 | 24（起動画面）／16（結果画面） |
| ブロック内の行間隔 | 10（約束の3行）／8（タイトルと説明）／6（見出しと本文） |
| 囲みの内側 | 18（起動画面の約束枠）／12（結果画面の各ボックス） |
| チップの内側 | 左右 10・上下 5 |
| コマンド枠の内側 | 6 |
| アイコンと文の間 | 10 |
| アイコンの占有幅 | 20（固定して文頭を揃える） |
| ウインドウ最小 | 640 × 580 |

4の倍数が基本。6・10・18・5 は行内の細かい調整にだけ出てくる。

### 4.2 サムネイルの余白

| 場所 | 実測 | キャンバス比 |
|---|---|---|
| 見出しの上 | 222〜237px | 24〜26% |
| 見出し → サブタイトル | 50〜42px | 約 5% |
| サブタイトル → タグ | 63〜46px | 約 6% |
| タグの下（下端まで） | 146〜135px | 約 15% |
| 左右の最小余白 | 132px | 約 7.6% |
| タグ枠の内側 | 左右 32〜43px・上下 22px | |

文字ブロック全体は、キャンバスの中央より **約4%下** に置いている。
上に余白を多めに取ると、文字の重さと釣り合う。

### 4.3 LPで使う値

使ってよい数字を固定する。

```
4 / 6 / 8 / 10 / 12 / 16 / 18 / 24 / 28 / 48 / 96
```

上の9つはアプリの実測値。48と96は24の倍数で、セクションの間隔にだけ使う。

| 場所 | 値 |
|---|---|
| セクション間 | 96px（狭い画面では 48px） |
| ブロック間 | 24px |
| 囲みの内側 | 24px |
| 行の間隔 | 10px |
| 左右の外周 | 28px |
| 本文カラムの最大幅 | **840px** |

840pxは実測値。アプリのウインドウ幅 891 から左右の余白 28×2 を引いた 835 に合わせている。
これより広くすると、1行が長くなって「一文は短く」の効果が消える。

---

## 5. 角丸・枠線

| 場所 | 半径 |
|---|---|
| 起動画面の約束枠 | 12 |
| 結果画面の各ボックス | 8 |
| コマンド表示 | 5 |
| チップ・バッジ | 完全な丸（Capsule） |
| サムネのタグ枠 | 0（直角） |

枠線を持つのはサムネのタグだけ。白・2px相当・塗りなし・直角。
アプリ側のブロックは枠線を持たず、背景色の差だけで区切っている。

LPも同じにする。**囲みは背景色で作り、枠線を足さない。**
アプリは結果画面で細い区切り線を1本だけ使っているが、LPでは余白で分ける。
線を増やすほど装飾に近づく。

影は使わない。アプリに1つも無い。

---

## 6. アイコン

### 6.1 アプリで使っているもの

SF Symbols のみ。すべて線画（アウトライン）。

| 記号 | 意味 |
|---|---|
| `key.viewfinder` | 起動画面のシンボル（56pt・オレンジ） |
| `wifi.slash` | 外部に送信しない |
| `eye.slash` | 中身を表示しない |
| `hand.raised` | 書き換えない |
| `exclamationmark.triangle.fill` | いますぐ対処（唯一の塗りつぶし） |
| `eye` | 確認した方がいい |
| `checkmark.circle` / `.fill` | そのままでよい／0件 |
| `flask` | テスト用の可能性 |
| `questionmark.circle` | 誤検出の可能性 |
| `arrow.clockwise` | 再発行の手順 |
| `lock` | 権限が足りず読めなかった |
| `doc.on.doc` | コマンドをコピー |
| `chevron.right` / `.down` | 開閉 |

### 6.2 扱いのルール

- **文の左に置く目印。単独で意味を持たせない。** アイコンを消しても文だけで通じる状態にする
- 幅を20ptに固定して、複数行の文頭を揃える
- 大きく使うのは起動画面（56pt）と失敗画面（40pt）だけ。それ以外は本文と同じ大きさ
- 色はアクセント・文字色・危険度の色のどれか1つ。1つのリストの中で混ぜない
- 塗りつぶしは `exclamationmark.triangle.fill` と `checkmark.circle.fill` だけ

### 6.3 LPでの置き換え

SF Symbols はWebで使えない。線の太さが近いアイコンセット（Lucide か Phosphor の Regular）で置き換える。
SVGを直接埋め込み、アイコンフォントを読み込まない。

置き換えても上のルールは変えない。線画・本文と同じ大きさ・文の左・幅を揃える。

### 6.4 アプリアイコン

`scripts/make_icon.swift` が生成している。

- 板：1024キャンバスに 824角の角丸（半径185）
- 背景：濃紺のグラデーション `#21336B` → `#0D1738`（上から下）
- 記号：`key.fill` を白で、板の 52% の大きさ

**LPではこのアイコンをそのまま置く。作り直さない。**

このアイコンだけがグラデーションを使っている。既存の唯一の例外として扱い、
LPの他の場所（背景・ボタン・文字・囲み）にグラデーションを持ち込まない。

---

## 7. 使わないもの

既存のアプリとサムネイル2枚に、以下は1つも出てこない。LPでも出さない。

**人物・イラスト**
- 人物写真、イラスト、キャラクター、アバター、手描き風の絵
- 「安心している人」「困っている人」の類の説明イラスト

**装飾**
- 影（drop-shadow / box-shadow）
- 光彩、ぼかし、ネオン
- 斜めの帯、波形の区切り、背景のパターン
- パララックス、スクロール連動アニメーション
- 絵文字

**グラデーション**
- 背景・ボタン・文字・囲みに使わない
- アプリアイコンの濃紺グラデーションだけが例外。それは画像として置くだけ

**色**
- オレンジ・白・グレー以外を増やさない
- 緑・黄は状態を示すときだけ。装飾に使わない
- 赤を使わない

**動き**
- スキャン中の進行表示以外にアニメーションを付けない
- ホバーで拡大・回転・浮き上がる、をしない（色の変化だけにする）

**スクリーンショットの加工**
- 枠を付けない、影を付けない、傾けない
- Macの筐体モックアップに載せない
- 部分を丸で囲む、矢印を引く、拡大鏡を重ねる、をしない
- `docs/screenshot*.png` をそのまま貼る

**その他**
- カプセル型のボタン（カプセルはチップだけ）
- 「今すぐダウンロード!!」型の強い装飾ボタン
- カウントダウン、残り枠数、ポップアップ

---

## 8. 文章の書き方

正本は `x-research-ai/style_profile.md`。LPに関係する部分を抜く。

### 8.1 書き方

- **結論を最初の一文に書く**
- 一文は40字以内。一文に1つのことだけ書く
- 中学生でも話の流れを追える言葉で書く。内容のレベルは下げない
- カタカナの専門用語は、直後に短い説明を付ける
  - アプリでの実例：`APIキー(AIを使うための、あなた専用の合鍵)`
- 抽象語を具体的な物・行動・数字に置き換える
- 一人称は「僕」
- 上から教える口調にしない
- していないことを、実践済みのように書かない
- 同じ語尾を3回以上続けない
- 2〜3文ごとに空行を入れる

### 8.2 禁止語

一覧は `style_profile.md` の8章にある。LPで出やすいものを挙げる。

> 最適化 / 本質 / 可能性 / 課題 / 解像度 / シームレス / 価値提供 / 革新的 / 画期的 /
> 不可欠 / 核心的 / 鍵となる / 包括的 / 掘り下げる / 言語化する / まとめると / 重要なのは /
> 非常に / 極めて / と言えるだろう / 3つの観点から

使ったら、具体的な物・行動・数字に置き換えて書き直す。

### 8.3 LPの最初の画面に必ず置くもの

アプリの起動画面が約束している3つを、そのままの言い回しで置く。

1. 外部に一切送信しません。全部このMacの中だけで動きます。
2. キーの中身は画面にも出しません。先頭7文字と文字数だけです。
3. 読むだけです。ファイルの中身は書き換えません。

「無料です」も最初の画面に置く。

### 8.4 数字の見せ方

数字だけを大きくして、単位と説明文は小さいまま横に置く。
結果画面もサムネも同じ形になっている。

```
[ 24 ]ファイルに散らばってた
 ↑オレンジ・大 ↑白・小
```

数字を四捨五入しない。`31,705 ファイルを確認しました` のように、測った値をそのまま出す。

> 3段階分離・危険度3層の定義の正本は README.md「数え方」節。
> ここには見せ方（配置・サイズ・強調）のみを書く。

---

## 9. LP実装用のまとめ

```css
:root {
  /* 色 */
  --bg:            #000000;
  --surface:       #242A2E;
  --surface-inner: #2F3438;
  --chip:          #393F42;
  --note-bg:       #3E362E;
  --text:          #FFFFFF;
  --text-sub:      #9C9FA1;
  --accent:        #F98517;

  /* フォント */
  --font-sans: -apple-system, BlinkMacSystemFont, "Hiragino Sans",
               "Noto Sans JP", sans-serif;
  --font-mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;

  /* 文字サイズ */
  --fs-hero:     clamp(32px, 7.5vw, 96px);
  --fs-section:  28px;
  --fs-number:   56px;
  --fs-subhead:  19px;
  --fs-body:     17px;
  --fs-small:    15px;   /* 下限。これより小さくしない */

  /* 行間 */
  --lh-head: 1.35;
  --lh-body: 1.8;

  /* 余白 */
  --sp-4: 4px;   --sp-6: 6px;   --sp-8: 8px;   --sp-10: 10px;
  --sp-12: 12px; --sp-16: 16px; --sp-18: 18px; --sp-24: 24px;
  --sp-28: 28px; --sp-48: 48px; --sp-96: 96px;

  /* 角丸 */
  --r-lg: 12px;  /* 大きい囲み */
  --r-md: 8px;   /* 通常の囲み */
  --r-sm: 5px;   /* コマンド表示 */

  /* 幅 */
  --col-max: 840px;
}
```

### ページの骨格

| 位置 | 中身 | 見た目 |
|---|---|---|
| ヒーロー | サムネと同じ組み方。数字1つだけオレンジ | 背景 `--bg` |
| 3つの約束 | 起動画面の3行をそのまま | 囲み `--surface` |
| スクリーンショット | `docs/screenshot.png` を加工せずに貼る | 背景 `--bg` |
| 何を見つけるか | 検出する場所の一覧 | 囲み `--surface` |
| ダウンロード | 主ボタン1つ。オレンジ塗り・白文字 | 背景 `--bg` |
| 何をしないか | READMEの「このアプリがしないこと」 | 囲み `--surface` |

ボタンは1ページに1種類。オレンジ塗り・白文字・角丸8px・上下12px / 左右24px。
2つ目以降のリンクは、下線付きのテキストリンク（`#419CFF`）にする。

---

## 10. 迷ったときの判断

1. **その値はどこかに実在するか。** 実在しないなら、いちばん近い実在値を使う
2. **オレンジは1画面に1つか。** 2つ以上あるなら、どちらかを白に落とす
3. **消しても意味が通じるか。** 通じるなら、その飾りは消す
4. **文字は15px以上か。** 下回っているなら上げる
5. **アイコンを消しても文だけで分かるか。** 分からないなら文を直す

---

## 同期対象一覧

この DESIGN.md と同じ内容を書いている場所。**片方だけ直さないこと。**

**役割分担：定義（何を数えるか・どう分類するか）は README.md が正本。見た目（配置・サイズ・強調）は DESIGN.md が正本。**

| 相手 | 場所 | 同期する内容 | 対応する節 |
|---|---|---|---|
| README.md | 59行目付近（「数え方」節） | 3段階分離・危険度3層の定義 | §8.4 |
| README.md | 94行目付近（「並べ方」節） | 画面トップの数字が「いますぐ対処」の件数であること | §8.4（サイズは §3.1） |

直す時の順番は、**定義を変えるなら README.md → DESIGN.md**、
**見た目を変えるなら DESIGN.md → README.md**。先に正本の側を直す。

### 同期対象外

- `docs/release-notes/` —— 過去の記録につき同期対象外。
  リリース時点の数字と文言をそのまま残す。定義が変わっても遡って直さない。

---

最終更新: 2026-07-26
